<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h3>Users you may want to connect with:</h3>
    <% unless @possible_connections.any? %>
      <p>Looks like there is no one else to connect with.</p>
      <p>You should invite some of your friends to join!</p>
    <% else %>
      <table>
        <tr>
          <th>Name</th>
          <th>Action</th>
        </tr>
        <% @possible_connections.each do |person| %>
          <tr>
            <td><a href="/users/<%= person.id %>"><%= person.first_name %> <%= person.last_name %></a></td>
            <td><a href="/invitations/<%= person.id %>">Connect</a></td>
          </tr>
        <% end %>
      </table>
    <% end %>
  </body>
</html>
